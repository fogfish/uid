{lib_dirs, ["deps"]}.

{deps, [
   % fogfish erlang toolkit archive
   {feta, ".*",
      {git, "https://github.com/fogfish/feta", {branch, master}}
   },

   % process term storage
   {pts, ".*",
      {git, "https://github.com/fogfish/pts", {branch, master}}
   }
]}.
